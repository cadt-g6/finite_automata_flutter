import 'package:flutter/material.dart';

class FaDetailScreen extends StatelessWidget {
  const FaDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Add FA"),
      ),
    );
  }
}
